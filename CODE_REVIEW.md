# Сениор-ревью кода — Telegram-Bot-TESTDM

> Полный отчёт ревью (10 областей) + статус исправлений. Сохранён, чтобы не потерять при компакте контекста.
> Порядок работы: чиним 🔴 → 🟠 → 🟡, по одному, формат «обсудили решение → подтверждение → реализация».

**Шкала:** 🔴 критично (потеря данных / падение / дыра) · 🟠 существенно (баг в нечастом сценарии / риск на росте) · 🟡 мелочь (читаемость/стиль).
**Статус:** ✅ исправлено (коммит) · 🔧 в работе · ⬜ открыто.

## Прогресс
| Находка | Важность | Статус |
|---|---|---|
| 1. Токен бота в логах (httpx) | 🔴 | ✅ `4535d19` |
| 2. ffmpeg без таймаута | 🟠 | ✅ `76ba5d2` |
| 3. Файлы >20 МБ ломают обработку | 🟠 | ✅ `55e0ec3` |
| 4. Длинные расшифровки >4096 | 🟠 | ✅ `d0bf178` |
| 5+6. Привязка ответов (pending_clarification) | 🟠 | 🔧 проектируем «решение Y» (см. INTERACTION_FLOW.md) |
| 7. Неатомарная запись / гонка дозаписей | 🟠 | ⬜ |
| 8. Нет автотестов | 🟠 | ⬜ |
| 🟡-мелочи (см. ниже) | 🟡 | ⬜ |

---

## 1. Корректность логики
- **🟠 [5] Перезапись при нескольких вопросах.** `pending_clarification[user]=qid` хранит один ожидаемый ответ; два вопроса подряд → первый теряет ответ. → решается «Y». 🔧
- **🟠 [6] Мис-атрибуция.** «Следующее сообщение = ответ» — догадка; спонтанное сообщение приклеивается к вопросу. → «Y». 🔧
- **🟡 Нет верхнего предела числа детей** (`get_children_count`): ввод «9999» → застревание в цикле. ⬜
- **🟡 Нет лимита/проверки пустоты ФИО** (`get_name`). ⬜
- **🟡 «Сирота» в карточке:** если запись-вопрос исчезнет, а ответ есть — `card_renderer` не отрисует осиротевший ответ. ⬜
- **🟡 `/cancel` завершает диалог** (END) — после него сообщения и ответы на уточнения не ловятся (рассогласование с «постоянным» `/stop`). ⬜

## 2. Надёжность и обработка ошибок
- **🟠 ffmpeg без таймаута** (`media_utils.py`): мог зависнуть навсегда, заняв поток пула. ✅ `76ba5d2` (FFMPEG_TIMEOUT=120).
- **🟠 Файлы >20 МБ** (`_download`): `get_file()` падал → сообщение терялось. ✅ `55e0ec3` (проверка размера + вежливый отказ + заглушка `oversized` + пометка исследователю).
- **🟠 Длинные расшифровки >4096** ломали ответ юзеру и пометку в тему. ✅ `d0bf178` (превью ~500 символов респонденту; полный текст — в card.md и в тему, режется на части).
- **🟡 Нет ретраев транскрибации** при сетевом сбое (данные не теряются, текст пропадает). ⬜

## 3. Целостность данных
- **🟠 [7] Неатомарная запись** `profile.json` / `card.md` (`open("w")` — усечение+запись): обрыв процесса → битый файл (profile = «не зарегистрирован»). Фикс: temp-файл + `os.replace` (как для token.json). ⬜
- **🟠 [7] Гонка одновременных дозаписей** в `feedback.jsonl`: два быстрых сообщения от одного юзера → параллельные `append`; на Windows атомарность не гарантирована → возможна «склейка» строки → `_read_feedback` молча её пропустит (тихая потеря). Фикс: lock на запись на пользователя. ⬜

## 4. Параллелизм и гонки
- **✅ Гонка Drive** — закрыта ранее (сериализация + атомарный токен).
- **🟡 Мутации общих карт `bot_data`** из параллельных хендлеров без явной синхронизации (риск низкий, не нулевой). ⬜

## 5. Безопасность
- **🔴 Токен бота в логах** (httpx писал каждый запрос с токеном в URL). ✅ `4535d19` (httpx→WARNING + фильтр-редактор `<REDACTED>` по шаблону токена и литералам).
- **🟠 PII в открытом виде** (ФИО/возраст/дети/голоса) локально и в Drive; нет политики хранения/удаления и фиксации согласия. ⬜ (политика/юр., не только код).
- **🟡 Whitelist только при регистрации** — удалённый из whitelist уже зарегистрированный продолжит слать фидбек. ⬜
- **🟡 `/chatid` доступен всем** — chat_id не секрет, но команду стоит убрать/ограничить после настройки. ⬜
- **✅ Хорошо:** subprocess list-args (нет injection), экранирование Drive-запросов, `html.escape` для динамики, санитизация расширений, scope `drive.file`.

## 6. Производительность и масштабируемость
- **🟡 O(n²) на диалог:** `regenerate_card` на КАЖДОЕ сообщение перечитывает весь `feedback.jsonl`, переписывает и перезаливает `card.md`. Для исследования терпимо; на росте — узкое место. Фикс: дебаунс / инкрементально. ⬜
- **🟡 Безграничный рост `msgmap` / `question_group_msg`** в pickle. Фикс: периодическая чистка. ⬜

## 7. Поддерживаемость и качество кода
- **🟡 Обращение к «приватным» функциям между модулями** (`drive_sync._get_service` из CLI-скриптов). ⬜
- **🟡 Возможный мёртвый код** — `save_text_feedback` после рефакторинга. ⬜
- **🟡 «Магические строки»** ключей `bot_data` повторяются — вынести в константы. ⬜
- **🟡 Восстановление после потери pickle неполное** — `topic_pinned` не восстанавливается из profile (reply на сводку перестанет распознаваться; `/ask` работает). ⬜

## 8. Тесты
- **🟠 [8] Автотестов нет вообще.** Главный риск регрессий. Минимум покрыть: `transcriber._extract_texts` (NDJSON), `card_renderer.render_card` (треды), `handlers._safe_ext`/`_is_allowed`/`_clip`, `researcher_bridge._topic_name`/`_split_text`. ⬜

## 9. Конфигурация, логирование, развёртывание
- **🔴/🟠** токен в логах — ✅ (см. п.5).
- **🟡 Логи только в stdout**, без файла/ротации (в облаке journald — ок). ⬜
- **🟡 Смешение `print` и `logging`** в `bot.py`. ⬜

## 10. Документация vs код
- **🟡 Спека §5 утверждала «обрезка до ~3500»** — теперь реализовано (превью 500 / нарезка в тему), формулировку в спеке при случае выровнять. ⬜
